[package]
name = "bonsai-wgpu"
version = "0.1.0"
edition = "2021"

[lib]
name = "bonsai_wgpu"
path = "src/lib.rs"

[[bin]]
name = "bonsai-wgpu"
path = "src/bin/bonsai-wgpu.rs"
required-features = ["bench-internals"]

[features]
default = []
# Exposes bench/microbench utilities under `bonsai_wgpu::__bench`. Used by the
# bundled `bonsai-wgpu` binary; not part of the stable public API.
bench-internals = []

[dependencies]
wgpu = "29"
pollster = "0.3"
bytemuck = { version = "1.16", features = ["derive"] }
half = { version = "2", features = ["bytemuck"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
env_logger = "0.11"
futures-intrusive = "0.5"

[dev-dependencies]
pollster = "0.3"
bytemuck = { version = "1.16", features = ["derive"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
